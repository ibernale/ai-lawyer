# apps/web

Next.js 14 (App Router) frontend for the lex-agents platform. Built with
TypeScript, Tailwind CSS, and shadcn/ui. Provides the legal consultation
interface: query input, response rendering with verified citations, and
session history. Communicates exclusively with `apps/api` over REST.
